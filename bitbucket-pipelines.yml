image: python:3.11

pipelines:
  default:
    - step:
        name: Tests
        caches:
          - pip
        script:
          - python -V
          - pip install -r requirements.txt
          - python -m unittest discover -s tests -v
