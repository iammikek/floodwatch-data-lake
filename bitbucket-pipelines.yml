image: python:3.11

pipelines:
  default:
    - step:
        name: Tests
        caches:
          - pip
        script:
          - python -V
          - pip install -r requirements.txt
          - mkdir -p test-reports
          - python -m xmlrunner discover -s tests -v -o test-reports
        artifacts:
          - test-reports/**
